{
  "version": 2,
  "build": {
    "env": {
      "MONGO_URI": "@notes-app-mongo-uri",
      "JWT_SECRET": "@notes-app-jwt-secret"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js",
      "methods": ["GET", "POST", "PUT", "DELETE"]
    }
  ]
}